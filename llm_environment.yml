name: decision_making
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip:
      # PyTorch (CUDA 13)
      - torch==2.11.0
      # LLM stack
      - transformers==5.5.0
      - accelerate==1.13.0
      - bitsandbytes==0.49.2
      - tokenizers==0.22.2
      - safetensors==0.7.0
      - huggingface-hub==1.9.0
      # Cloud LLM
      - anthropic
      # LiDAR processing (scene abstraction)
      - open3d==0.18.0
      # Utilities
      - numpy==2.2.6
      - pyyaml==6.0.3
      - tqdm==4.67.3
      - easydict==1.13
